const { test, expect } = require('@playwright/test');
 
/*
test('Login Page', async ({ browser }) => {
    const context = await browser.newContext();
    const page = context.newPage;
    await page.goto("https://access.amexgbt.com/");


});
*/

/*
 test('Login Page', async ({page }) => {
  await  page.goto ("https://access.amexgbt.com/")
  const pagetitle = await page.title();
  console.log( 'Page Titleis: ' +pagetitle)
  await expect(page).toHaveTitle(pagetitle)
  await page.locator ("//h2[contains(text(),'Sign In')]").type("test.user202@yopmail.com");
  await page.locator("input#okta-signin-submit2").click();
  await page.waitForSelector ("input#okta-signin-password")
  const element_Pwd = await page.locator("input#okta-signin-password")
  await element_Pwd.type("Amex@321");
  await page.locator("input#okta-signin-submit").click()
  await page.waitForSelector ("//input[@type='submit']")
  const elementSenMeCODE = await page.locator("//input[@type='submit']")
  await elementSenMeCODE.click()
  await page.waitForSelector ("#input22")
  const verify = await page.locator("#input22")
  verify.type("243803")
  page.pause(5000)
  await page.locator("input[value='Verify']").click()
  await page.waitForSelector ("//label[text() ='I accept']")
  const acceptCheckBox =  page.locator("//label[text() ='I accept']")
  acceptCheckBox.click()
  expect(acceptCheckBox).toBeChecked();
  
   



  page.goto("https://yopmail.com/en/")
  await page.locator("#login").type("test.user202")
  await page.locator(".material-icons-outlined.f36").click()
  const inputElement = await page.locator("//td [@align='left'])[6]");
  const inputValue = await inputElement.inputValue();
  console.log(inputValue);
page.pause(5000)

  */


    

    











//});
 