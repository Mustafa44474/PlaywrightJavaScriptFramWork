const { test, expect } = require('@playwright/test');
/*

test("Child will in Playwrigh", async ({ browser }) => {
    const context = await browser.newContext();
    const page = await context.newPage();


    await page.goto("https://rahulshettyacademy.com/loginpagePractise/")
    const documentlinkBlening = page.locator("[href*='documents-request']")

    const newPage = await Promise.all([
        context.waitForEvent('page'),
        documentlinkBlening.click()

    ])
    await newPage.locator("//a[text()='JOIN NOW']").click()
    newPage.pause()
    // To Swich back from child window to Parent use 
    page.locator("test").click()
})
*/