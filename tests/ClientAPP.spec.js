
const { test, expect } = require('@playwright/test');
/*
test("Dynamic text Conten", async ({ page }) => {
    page.goto("https://rahulshettyacademy.com/client/auth/login")

    const emailField = page.locator("input#userEmail")
    const passwordField = page.locator("#userPassword")
    const loginButton = page.locator("#login")
    const productTitles = page.locator("div.card-body b")
    await emailField.fill("anshika@gmail.com")
    await passwordField.fill("Iamking@000")
    await loginButton.click()
    await page.waitForLoadState('networkidle')
    console.log('Page Product title Text:  ' + await productTitles.allTextContents());

});

*/